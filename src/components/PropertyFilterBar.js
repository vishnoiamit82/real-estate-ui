import React from 'react';
import PropertyFilterLayout from './PropertyFilterLayout';

const PropertyFilterBar = (props) => {
  return <PropertyFilterLayout {...props} />;
};

export default PropertyFilterBar;
